{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "lerna bootstrap",
    "prepare": "husky install",
    "build": "cd packages/sdk && yarn build",
    "release": "yarn build && lerna publish from-package -y"
  },
  "devDependencies": {
    "@meshsdk/core": "^1.3.0",
    "lerna": "^5.6.2",
    "lucid-cardano": "^0.8.4"
  }
}
